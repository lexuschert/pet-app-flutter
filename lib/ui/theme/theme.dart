import 'package:flutter/material.dart';

const primaryColor = Color.fromRGBO(0, 207, 170, 1);

final darkData = ThemeData(
  useMaterial3: true,
  primaryColor: primaryColor,
);

final lightData = ThemeData(
  useMaterial3: true,
  primaryColor: primaryColor,
);
